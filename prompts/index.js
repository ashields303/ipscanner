const mainPrompt = require("./mainPrompt");
const helpPrompt = require("./helpPrompt");
const scanPrompt = require("./scanPrompt");

module.exports = {
  mainPrompt,
  helpPrompt,
  scanPrompt,
};
